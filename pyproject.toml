[project]
name = "shopping_env"
version = "0.1.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0",
    "openai",
    "requests"
]

[project.scripts]
server = "server.app:main"
